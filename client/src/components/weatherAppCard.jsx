import React from "react";
import WeatherAppForm from "./weatherAppForm";

export default function WeatherAppCard(props) {

    return (
      <>
        {/* <h1>City: {props.weather.name}</h1>
        <h2>Temperature: {props.weather.main.temp}</h2>
        <p>Weather: {props.weather.weather[0].description}</p> */}
      </>
    );
  }
